import { Activity, Archive, BookPlus, Check, ClipboardCheck, Download, Eye, EyeOff, FileArchive, Gamepad2, MessageSquare, Plus, RotateCcw, Save, ShieldCheck, Trash2, Upload, UserCog, UsersRound, X } from 'lucide-react'
import { useMemo, useState } from 'react'
import { createId, getLearningState, saveLearningState, type LearningState, type StudentAccountRecord } from '../../../lib/studentLearningStore'

type Section = 'teachers' | 'accounts' | 'courseware' | 'assessments' | 'assignments' | 'submissions' | 'moderation' | 'activities'
const sections = [
  {id:'teachers' as const,label:'教师账号',icon:ShieldCheck},{id:'accounts' as const,label:'学生账号',icon:UserCog},{id:'courseware' as const,label:'网页课件',icon:FileArchive},{id:'assessments' as const,label:'测评管理',icon:ClipboardCheck},{id:'assignments' as const,label:'作业发布',icon:BookPlus},
  {id:'submissions' as const,label:'作业评分',icon:Save},{id:'moderation' as const,label:'广场审核',icon:MessageSquare},{id:'activities' as const,label:'活动报名',icon:Activity},
]

export default function StudentPortalPanel({classId}:{classId:string}){
  const [section,setSection]=useState<Section>('accounts');const [state,setState]=useState(getLearningState)
  const persist=(next:LearningState)=>{saveLearningState(next);setState(next)}
  if(!classId)return <div className="admin-empty-state"><Gamepad2/><h3>请先选择班级</h3><p>学生账号和所有学习内容都按班级管理。</p></div>
  return <div><div className="admin-page-heading"><div><small>STUDENT PORTAL</small><h1>学生端管理</h1><p>统一管理账号、课件、测评、作业与交流广场。</p></div><a className="admin-primary" href="#/student/login" target="_blank" rel="noreferrer"><Eye/>查看学生端</a></div><div className="portal-admin-tabs">{sections.map((item)=><button type="button" key={item.id} className={section===item.id?'active':''} onClick={()=>setSection(item.id)}><item.icon/>{item.label}</button>)}</div>
    {section==='teachers'&&<TeachersSection classId={classId} state={state} persist={persist}/>} {section==='accounts'&&<AccountsSection classId={classId} state={state} persist={persist}/>} {section==='courseware'&&<CoursewareSection classId={classId} state={state} persist={persist}/>} {section==='assessments'&&<AssessmentsSection classId={classId} state={state} persist={persist}/>} {section==='assignments'&&<AssignmentsSection classId={classId} state={state} persist={persist}/>} {section==='submissions'&&<SubmissionsSection state={state} persist={persist}/>} {section==='moderation'&&<ModerationSection state={state} persist={persist}/>} {section==='activities'&&<ActivitiesSection state={state} persist={persist}/>}</div>
}

function TeachersSection({classId,state,persist}:{classId:string;state:LearningState;persist:(state:LearningState)=>void}){
  const [form,setForm]=useState({name:'',email:''})
  const add=(event:React.FormEvent)=>{event.preventDefault();const next=getLearningState();next.teachers.push({id:createId('teacher'),name:form.name,email:form.email,role:'teacher',classIds:[classId],active:true});persist(next);setForm({name:'',email:''})}
  const toggle=(id:string)=>{const next=getLearningState();const item=next.teachers.find((entry)=>entry.id===id);if(item&&item.role!=='owner')item.active=!item.active;persist(next)}
  return <section className="portal-admin-card"><header><div><ShieldCheck/><span><strong>教师账号与班级权限</strong><small>主管理员管理全部内容，普通教师仅管理分配给自己的班级。</small></span></div></header><form className="quick-create-row teacher" onSubmit={add}><label>教师姓名<input value={form.name} onChange={(e)=>setForm({...form,name:e.target.value})} required/></label><label>登录邮箱<input type="email" value={form.email} onChange={(e)=>setForm({...form,email:e.target.value})} required/></label><button className="admin-primary"><Plus/>添加教师</button></form><table className="admin-table"><thead><tr><th>教师</th><th>登录邮箱</th><th>角色</th><th>管理范围</th><th>状态</th></tr></thead><tbody>{state.teachers.map((item)=><tr key={item.id}><td><strong>{item.name}</strong></td><td>{item.email}</td><td>{item.role==='owner'?'主管理员':'班级教师'}</td><td>{item.classIds.includes('*')?'全部班级':'当前班级'}</td><td><button type="button" disabled={item.role==='owner'} className={`account-status ${item.active?'active':''}`} onClick={()=>toggle(item.id)}>{item.active?'可登录':'已停用'}</button></td></tr>)}</tbody></table></section>
}

function AccountsSection({classId,state,persist}:{classId:string;state:LearningState;persist:(state:LearningState)=>void}){
  const [showForm,setShowForm]=useState(false);const [form,setForm]=useState({studentName:'',username:'',password:'123456'});const [visible,setVisible]=useState<string[]>([])
  const accounts=state.accounts.filter((item)=>item.classIds.includes(classId)||item.classIds.includes('python-summer'))
  const save=(event:React.FormEvent)=>{event.preventDefault();if(state.accounts.some((item)=>item.username===form.username.trim()))return window.alert('这个学生账号已存在。');const next=getLearningState();next.accounts.push({id:createId('student'),studentName:form.studentName.trim(),username:form.username.trim(),password:form.password,classIds:[classId],active:true});persist(next);setForm({studentName:'',username:'',password:'123456'});setShowForm(false)}
  const reset=(account:StudentAccountRecord)=>{const password=window.prompt(`为 ${account.studentName} 设置新密码：`,'123456');if(!password)return;if(password.length<6)return window.alert('密码至少需要 6 位。');const next=getLearningState();const target=next.accounts.find((item)=>item.id===account.id);if(target)target.password=password;persist(next);window.alert('密码已重置。')}
  const toggle=(id:string)=>{const next=getLearningState();const target=next.accounts.find((item)=>item.id===id);if(target)target.active=!target.active;persist(next)}
  return <section className="portal-admin-card"><header><div><UsersRound/><span><strong>学生登录账号</strong><small>学生可以自行修改密码，忘记密码由教师在这里重置。</small></span></div><button type="button" className="admin-primary" onClick={()=>setShowForm(!showForm)}><Plus/>创建账号</button></header>{showForm&&<form className="quick-create-row" onSubmit={save}><label>学生姓名<input value={form.studentName} onChange={(e)=>setForm({...form,studentName:e.target.value})} required/></label><label>登录账号<input value={form.username} onChange={(e)=>setForm({...form,username:e.target.value})} required/></label><label>初始密码<input value={form.password} minLength={6} onChange={(e)=>setForm({...form,password:e.target.value})} required/></label><button className="admin-primary">保存账号</button></form>}<table className="admin-table"><thead><tr><th>学生</th><th>登录账号</th><th>当前密码</th><th>所属班级</th><th>状态</th><th/></tr></thead><tbody>{accounts.map((item)=><tr key={item.id}><td><strong>{item.studentName}</strong></td><td><code>{item.username}</code></td><td><button type="button" className="password-view" onClick={()=>setVisible((list)=>list.includes(item.id)?list.filter((id)=>id!==item.id):[...list,item.id])}>{visible.includes(item.id)?<EyeOff/>:<Eye/>}{visible.includes(item.id)?item.password:'••••••'}</button></td><td>当前班级</td><td><button type="button" className={`account-status ${item.active?'active':''}`} onClick={()=>toggle(item.id)}>{item.active?'可登录':'已停用'}</button></td><td><button type="button" className="table-text-button" onClick={()=>reset(item)}><RotateCcw/>重置密码</button></td></tr>)}</tbody></table></section>
}

function CoursewareSection({classId,state,persist}:{classId:string;state:LearningState;persist:(state:LearningState)=>void}){
  const [title,setTitle]=useState('');const [file,setFile]=useState<File|null>(null);const [openAt,setOpenAt]=useState(new Date().toISOString().slice(0,16))
  const packages=state.coursewarePackages.filter((item)=>item.classId===classId||item.classId==='python-summer')
  const add=(event:React.FormEvent)=>{event.preventDefault();if(!file||!file.name.toLowerCase().endsWith('.zip'))return window.alert('请选择 ZIP 课件包。');const next=getLearningState();next.coursewarePackages.push({id:createId('courseware'),classId,title,fileName:file.name,openAt,published:true,createdAt:new Date().toISOString()});persist(next);setTitle('');setFile(null)}
  const toggle=(id:string)=>{const next=getLearningState();const item=next.coursewarePackages.find((entry)=>entry.id===id);if(item)item.published=!item.published;persist(next)}
  return <section className="portal-admin-card"><header><div><Archive/><span><strong>网页课件 ZIP 包</strong><small>ZIP 内包含 index.html、图片、音频和脚本；正式部署后自动解压到隔离目录。</small></span></div></header><form className="courseware-upload-form" onSubmit={add}><label>课件名称<input value={title} onChange={(e)=>setTitle(e.target.value)} placeholder="例如：第 4 课 海龟画笔出发" required/></label><label>开放时间<input type="datetime-local" value={openAt} onChange={(e)=>setOpenAt(e.target.value)} required/></label><label className="zip-picker"><Upload/>{file?file.name:'选择 ZIP 课件包'}<input hidden type="file" accept=".zip,application/zip" onChange={(e)=>setFile(e.target.files?.[0]||null)}/></label><button className="admin-primary">上传并发布</button></form><div className="admin-list-stack">{packages.map((item)=><div key={item.id}><span className="file-tile"><FileArchive/></span><div><strong>{item.title}</strong><small>{item.fileName} · 开放时间 {item.openAt.replace('T',' ')}</small></div><button type="button" className={item.published?'published':'unpublished'} onClick={()=>toggle(item.id)}>{item.published?<><Eye/>已发布</>:<><EyeOff/>已下架</>}</button></div>)}</div></section>
}

function AssessmentsSection({classId,state,persist}:{classId:string;state:LearningState;persist:(state:LearningState)=>void}){
  const [building,setBuilding]=useState(false);const [form,setForm]=useState({title:'',description:'',durationMinutes:30,maxAttempts:1,endAt:'2027-08-30T22:00'});const [questions,setQuestions]=useState([{id:createId('question'),type:'choice',title:'',options:'',answer:'',points:10}])
  const items=state.assessments.filter((item)=>item.classId===classId||item.classId==='python-summer')
  const toggle=(id:string)=>{const next=getLearningState();const item=next.assessments.find((entry)=>entry.id===id);if(item)item.published=!item.published;persist(next)}
  const save=(event:React.FormEvent)=>{event.preventDefault();if(questions.some((item)=>!item.title.trim()))return window.alert('请填写所有题目内容。');const next=getLearningState();next.assessments.push({id:createId('assessment'),classId,...form,kind:'course',startAt:new Date().toISOString().slice(0,16),published:true,questions:questions.map((item)=>({id:item.id,type:item.type as 'choice'|'true_false'|'blank'|'programming',title:item.title,options:item.type==='choice'?item.options.split(/[,，]/).map((value)=>value.trim()).filter(Boolean):item.type==='true_false'?['正确','错误']:undefined,answer:item.type==='programming'?undefined:item.answer,points:Number(item.points)}))});persist(next);setBuilding(false);setForm({title:'',description:'',durationMinutes:30,maxAttempts:1,endAt:'2027-08-30T22:00'});setQuestions([{id:createId('question'),type:'choice',title:'',options:'',answer:'',points:10}])}
  return <section className="portal-admin-card"><header><div><ClipboardCheck/><span><strong>测评试卷</strong><small>支持选择、判断、程序填空和人工评分编程题。</small></span></div><button type="button" className="admin-primary" onClick={()=>setBuilding(!building)}><Plus/>新建测评</button></header>{building&&<form className="assessment-builder" onSubmit={save}><div className="builder-meta"><label>试卷名称<input value={form.title} onChange={(e)=>setForm({...form,title:e.target.value})} required/></label><label>考试时长（分钟）<input type="number" min="1" value={form.durationMinutes} onChange={(e)=>setForm({...form,durationMinutes:Number(e.target.value)})}/></label><label>允许作答次数<input type="number" min="1" value={form.maxAttempts} onChange={(e)=>setForm({...form,maxAttempts:Number(e.target.value)})}/></label><label>截止时间<input type="datetime-local" value={form.endAt} onChange={(e)=>setForm({...form,endAt:e.target.value})}/></label></div><label>测评说明<input value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})}/></label><div className="builder-questions">{questions.map((question,index)=><div key={question.id}><span>题目 {index+1}</span><select value={question.type} onChange={(e)=>setQuestions((items)=>items.map((item)=>item.id===question.id?{...item,type:e.target.value}:item))}><option value="choice">选择题</option><option value="true_false">判断题</option><option value="blank">程序填空题</option><option value="programming">编程题</option></select><input className="question-title-input" value={question.title} onChange={(e)=>setQuestions((items)=>items.map((item)=>item.id===question.id?{...item,title:e.target.value}:item))} placeholder="输入题目内容"/>{question.type==='choice'&&<input value={question.options} onChange={(e)=>setQuestions((items)=>items.map((item)=>item.id===question.id?{...item,options:e.target.value}:item))} placeholder="选项用逗号隔开"/>}{question.type!=='programming'&&<input value={question.answer} onChange={(e)=>setQuestions((items)=>items.map((item)=>item.id===question.id?{...item,answer:e.target.value}:item))} placeholder="正确答案"/>}<input type="number" min="1" value={question.points} onChange={(e)=>setQuestions((items)=>items.map((item)=>item.id===question.id?{...item,points:Number(e.target.value)}:item))}/><button type="button" onClick={()=>setQuestions((items)=>items.filter((item)=>item.id!==question.id))}><Trash2/></button></div>)}</div><footer><button type="button" onClick={()=>setQuestions((items)=>[...items,{id:createId('question'),type:'choice',title:'',options:'',answer:'',points:10}])}><Plus/>添加题目</button><button className="admin-primary"><Save/>保存并发布</button></footer></form>}<div className="admin-list-stack">{items.map((item)=><div key={item.id}><span className="file-tile assessment"><ClipboardCheck/></span><div><strong>{item.title}</strong><small>{item.questions.length} 道题 · {item.durationMinutes} 分钟 · 最多 {item.maxAttempts} 次</small></div><em>{state.assessmentSubmissions.filter((entry)=>entry.assessmentId===item.id).length} 份答卷</em><button type="button" className={item.published?'published':'unpublished'} onClick={()=>toggle(item.id)}>{item.published?'已发布':'未发布'}</button></div>)}</div></section>
}

function AssignmentsSection({classId,state,persist}:{classId:string;state:LearningState;persist:(state:LearningState)=>void}){
  const [form,setForm]=useState({title:'',description:'',dueAt:'2027-08-30T22:00',maxSubmissions:3})
  const add=(event:React.FormEvent)=>{event.preventDefault();const next=getLearningState();next.homework.push({id:createId('homework'),classId,...form,published:true});persist(next);setForm({title:'',description:'',dueAt:'2027-08-30T22:00',maxSubmissions:3})}
  const toggle=(id:string)=>{const next=getLearningState();const item=next.homework.find((entry)=>entry.id===id);if(item)item.published=!item.published;persist(next)}
  return <section className="portal-admin-card"><header><div><BookPlus/><span><strong>发布学生作业</strong><small>设置作业说明、截止时间和可提交次数。</small></span></div></header><form className="assignment-create" onSubmit={add}><label>作业标题<input value={form.title} onChange={(e)=>setForm({...form,title:e.target.value})} required/></label><label>作业说明<textarea rows={3} value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})} required/></label><div><label>截止时间<input type="datetime-local" value={form.dueAt} onChange={(e)=>setForm({...form,dueAt:e.target.value})}/></label><label>可提交次数<input type="number" min="1" value={form.maxSubmissions} onChange={(e)=>setForm({...form,maxSubmissions:Number(e.target.value)})}/></label><button className="admin-primary"><Plus/>发布作业</button></div></form><div className="admin-list-stack">{state.homework.filter((item)=>item.classId===classId||item.classId==='python-summer').map((item)=><div key={item.id}><span className="file-tile"><BookPlus/></span><div><strong>{item.title}</strong><small>截止 {item.dueAt.replace('T',' ')} · 可提交 {item.maxSubmissions} 次</small></div><em>{state.homeworkSubmissions.filter((entry)=>entry.homeworkId===item.id).length} 份提交</em><button type="button" className={item.published?'published':'unpublished'} onClick={()=>toggle(item.id)}>{item.published?'已发布':'已下架'}</button></div>)}</div></section>
}

function SubmissionsSection({state,persist}:{state:LearningState;persist:(state:LearningState)=>void}){
  const [drafts,setDrafts]=useState<Record<string,{score:string;feedback:string}>>({})
  const grade=(id:string)=>{const draft=drafts[id];if(!draft)return;const next=getLearningState();const item=next.homeworkSubmissions.find((entry)=>entry.id===id);if(item){item.score=Number(draft.score);item.feedback=draft.feedback}persist(next)}
  return <section className="portal-admin-card"><header><div><Save/><span><strong>学生作业评分</strong><small>查看代码与附件名称，填写分数和评语后学生可以修改并重交。</small></span></div></header>{state.homeworkSubmissions.length?<div className="submission-admin-list">{state.homeworkSubmissions.map((item)=><article key={item.id}><header><div><strong>{item.studentName}</strong><span>第 {item.attempt} 次提交</span></div><small>{new Date(item.submittedAt).toLocaleString('zh-CN')}</small></header><pre>{item.code}</pre><p>学生说明：{item.note||'无'} · 照片 {item.photos.length} 张 · 视频 {item.video||'无'}</p><div><input type="number" min="0" max="100" placeholder={item.score===null?'分数':String(item.score)} value={drafts[item.id]?.score||''} onChange={(e)=>setDrafts({...drafts,[item.id]:{score:e.target.value,feedback:drafts[item.id]?.feedback||item.feedback}})}/><input placeholder={item.feedback||'填写学习评语'} value={drafts[item.id]?.feedback||''} onChange={(e)=>setDrafts({...drafts,[item.id]:{score:drafts[item.id]?.score||String(item.score||''),feedback:e.target.value}})}/><button type="button" className="admin-primary" onClick={()=>grade(item.id)}><Save/>保存评分</button></div></article>)}</div>:<div className="portal-empty"><Save/><strong>暂时没有学生提交作业</strong><span>学生提交后会自动出现在这里。</span></div>}</section>
}

function ModerationSection({state,persist}:{state:LearningState;persist:(state:LearningState)=>void}){
  const pending=state.plazaPosts.filter((item)=>!item.approved)
  const approve=(id:string)=>{const next=getLearningState();const item=next.plazaPosts.find((entry)=>entry.id===id);if(item)item.approved=true;persist(next)}
  const remove=(id:string)=>{if(!window.confirm('确定删除这个作品吗？'))return;const next=getLearningState();next.plazaPosts=next.plazaPosts.filter((item)=>item.id!==id);persist(next)}
  const removeComment=(postId:string,commentId:string)=>{const next=getLearningState();const post=next.plazaPosts.find((item)=>item.id===postId);if(post)post.comments=post.comments.filter((item)=>item.id!==commentId);persist(next)}
  return <section className="portal-admin-card"><header><div><ShieldCheck/><span><strong>交流广场审核</strong><small>作品审核通过后才展示；评论无需审核，但教师可以删除。</small></span></div><b>{pending.length} 个待审核</b></header>{pending.map((post)=><article className="moderation-item" key={post.id}><div><span>{post.studentName.slice(-1)}</span><div><strong>{post.title}</strong><small>{post.studentName} · {post.description}</small></div></div><pre>{post.code||'未附代码'}</pre><footer><button type="button" className="approve" onClick={()=>approve(post.id)}><Check/>审核通过</button><button type="button" onClick={()=>remove(post.id)}><X/>删除</button></footer></article>)}<h3 className="comment-manage-title">已展示作品与评论</h3>{state.plazaPosts.filter((item)=>item.approved).map((post)=><div className="comment-manage" key={post.id}><strong>{post.title}</strong>{post.comments.map((comment)=><span key={comment.id}><b>{comment.studentName}</b>{comment.content}<button type="button" onClick={()=>removeComment(post.id,comment.id)}><Trash2/></button></span>)}</div>)}</section>
}

function ActivitiesSection({state,persist}:{state:LearningState;persist:(state:LearningState)=>void}){
  const [form,setForm]=useState({title:'',description:'',date:'2026-09-20',registrationType:'internal',externalUrl:''})
  const names=useMemo(()=>Object.fromEntries(state.accounts.map((item)=>[item.id,item.studentName])),[state.accounts])
  const add=(event:React.FormEvent)=>{event.preventDefault();const next=getLearningState();next.activities.push({id:createId('activity'),title:form.title,description:form.description,date:form.date,registrationType:form.registrationType as 'internal'|'external',externalUrl:form.externalUrl||undefined,registeredStudentIds:[]});persist(next);setForm({title:'',description:'',date:'2026-09-20',registrationType:'internal',externalUrl:''})}
  const exportList=(activityId:string)=>{const activity=state.activities.find((item)=>item.id===activityId);if(!activity)return;const csv='学生姓名,活动名称\n'+activity.registeredStudentIds.map((id)=>`"${names[id]||id}","${activity.title}"`).join('\n');const url=URL.createObjectURL(new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}));const link=document.createElement('a');link.href=url;link.download=`${activity.title}-报名名单.csv`;link.click();URL.revokeObjectURL(url)}
  return <section className="portal-admin-card"><header><div><Activity/><span><strong>比赛活动报名</strong><small>发布活动、查看站内报名名单并导出为 Excel 可打开的 CSV 表格。</small></span></div></header><form className="activity-create" onSubmit={add}><input value={form.title} onChange={(e)=>setForm({...form,title:e.target.value})} placeholder="活动名称" required/><input value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})} placeholder="活动说明" required/><input type="date" value={form.date} onChange={(e)=>setForm({...form,date:e.target.value})}/><select value={form.registrationType} onChange={(e)=>setForm({...form,registrationType:e.target.value})}><option value="internal">站内报名</option><option value="external">外部链接</option></select>{form.registrationType==='external'&&<input type="url" value={form.externalUrl} onChange={(e)=>setForm({...form,externalUrl:e.target.value})} placeholder="报名网址"/>}<button className="admin-primary"><Plus/>发布活动</button></form><div className="activity-admin-grid">{state.activities.map((item)=><article key={item.id}><span>{item.date}</span><h3>{item.title}</h3><p>{item.registrationType==='internal'?'站内报名':'外部报名链接'}</p><strong>{item.registeredStudentIds.length} 人已报名</strong><ul>{item.registeredStudentIds.map((id)=><li key={id}>{names[id]||id}</li>)}</ul>{item.registrationType==='internal'&&<button type="button" onClick={()=>exportList(item.id)}><Download/>导出名单</button>}</article>)}</div></section>
}
