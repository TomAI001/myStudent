# 网页课件积分接入说明

站外 AI 制作的网页课件需要在学生答题后，通过浏览器消息把结果发送给“咱们班的成长记录”。

## 必填规则

- 每道题必须有一个永久不变且不重复的 `questionId`。
- `correct` 表示是否答对。
- `points` 表示答对后获得的积分，答错时平台按 0 分处理。
- 平台只记录同一名学生第一次答对该题所得的积分，重复作答不会重复加分。

## 课件中的调用代码

```html
<script>
  function submitAnswer(questionId, correct, points) {
    window.parent.postMessage(
      {
        type: 'growth-courseware:score',
        questionId: questionId,
        correct: correct,
        points: points
      },
      window.location.origin
    )
  }
</script>
```

答题按钮示例：

```html
<button onclick="submitAnswer('lesson-02-question-01', true, 10)">
  挑战成功！
</button>
```

完整演示课件位于 `public/demo-courseware.html`。正式版上传 ZIP 课件包时，系统会自动解压并在隔离框架中打开入口 `index.html`。
